---
- name: 'Instala pacotes de dependencia do sistema operacional'
  apt:
    name: "{{ item }}"
    state: latest
  become: yes
  with_items:
    - mariadb-server
    - mariadb
    - python-mysqldb
  when: ansible_facts['distribution'] == "Ubuntu"
